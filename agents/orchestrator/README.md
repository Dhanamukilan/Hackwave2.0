# orchestrator
Coordinates Log Agent -> History Agent -> Git Agent -> Component/Owner Agent -> Evidence Agent -> RCA Agent -> Remediation Agent per the architecture; manages tool-call budget and evidence assembly before invoking the LLM.
