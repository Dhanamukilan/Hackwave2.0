# models
SQLAlchemy ORM models mirroring the Postgres schema (users, repositories, pipelines, builds, test_runs, tests, failures, fingerprints, commits, changed_files, components, owners, deployments, predictions, investigations, remediations, feedback).
