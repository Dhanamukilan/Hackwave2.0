# github_actions_adapter
Pulls workflow run + job + step data and logs via GitHub REST/GraphQL API; parses annotations; normalizes into the internal Build/TestRun schema. This is the REAL adapter wired to your GitHub repo.
