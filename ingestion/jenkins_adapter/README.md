# jenkins_adapter
Optional second CI/CD adapter implementing the same CIAdapter interface as github_actions_adapter, so the core pipeline is CI-system-agnostic. Stub first, implement after GitHub path is stable.
