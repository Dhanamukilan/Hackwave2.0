# evaluation
Precision/recall/F1/confusion matrix (classification), PR-AUC/ROC-AUC + calibration (prediction), silhouette score (clustering), top-1/top-3 accuracy (component/owner ID). All metrics computed from real held-out data — never fabricated.
