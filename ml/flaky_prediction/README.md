# flaky_prediction
Flaky-risk model trained on per-test execution history features (failure rate, pass-after-failure, rerun recovery, duration variance, env variance, etc). Time-aware (chronological) train/val/test split — never randomly shuffled.
